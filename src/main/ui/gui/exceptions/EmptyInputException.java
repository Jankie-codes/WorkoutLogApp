package ui.gui.exceptions;

//Represents an exception that is thrown when the user submits empty inputs
// (such as when attempting to add a set with no given weight input)
public class EmptyInputException extends Exception {
}
